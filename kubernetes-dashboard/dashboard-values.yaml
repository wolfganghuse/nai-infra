metrics-server:
  enabled: false
nginx:
  enabled: false
cert-manager:
  enabled: false
app:
  ingress:
    enabled: true
    hosts:
    - dashboard.gpt01dev01.cloudnative.nvdlab.net
    ingressClassName: nginx
    secretName: kubernetes-dashboard-certs
    issuer:
      name: letsencrypt-prod
      scope: cluster